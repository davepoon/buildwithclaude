---
description: One-command project initialization — analyzes stack, installs tools, generates multi-agent team
argument-hint: [--no-graph] [--no-caveman] [--no-agents] [--no-mcp] [--agents-only] [--graph-only] [--caveman-only] [--mcp-only] [--rebuild]
---

Arguments: $ARGUMENTS

Invoke the `init` skill and run its full process. Pass through the arguments above — the skill documents how each flag changes behavior. Do not ask the user for confirmation; execute and report results at the end.
